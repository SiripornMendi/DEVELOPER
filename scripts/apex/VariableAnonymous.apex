string firstVariable = 'I am the first variable';
system.debug(firstVariable);

firstVariable = 'Change the first variable';
system.debug(firstVariable);

string firstName = 'Siriporn ';
string lastName = 'Mendi';
string fullName = firstName + lastName;
system.debug('My name is ' + fullName);